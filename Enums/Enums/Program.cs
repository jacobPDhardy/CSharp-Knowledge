// An enum is a special "class" that represents a group of constants (unchangeable/read-only variables)

int rightNum = (int)Months.April; //You can access enum items with the dot syntax
Console.WriteLine(+rightNum);

int wrongNum = (int)WrongMonths.April; 
Console.WriteLine(wrongNum);

